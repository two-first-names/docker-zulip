---
name: Publish Docker Image to GHCR (PR)

on:
  push:
    branches:
      - arm-build

jobs:
  multiarch-image:
    uses: ./.github/workflows/build-and-push-ghcr-common.yml
    secrets: inherit
    with:
      allow-push-latest: true
